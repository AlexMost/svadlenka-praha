{
  "scripts": {
    "prettier": "prettier . --write",
    "build": "node src/build/build.mjs",
    "build-prod": "NODE_ENV=production node src/build/build.mjs $(git rev-parse HEAD)",
    "publish": "npm ci && npm run build-prod && cd public && git init && git commit --allow-empty -m 'update website' && git checkout -b gh-pages && git add . && git commit -am 'update website' && git push git@github.com:AlexMost/svadlenka-praha.git gh-pages --force && cd .. && git checkout main"
  },
  "devDependencies": {
    "prettier": "3.5.3"
  }
}
